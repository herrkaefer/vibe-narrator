
## Structure

### Bridge


### Narrator MCP Server


## Why this is implemented as an MCP server?

So that it can serve as a general purpose MCP server, not just for coding agent, or local usage.

## Why the bridge is needed?

Without bridge, we need to modify system prompt to let agent to use the MCP server.
